Employees Record System

--This project performs operations like Insert,Update,Delete and Display all employees.
--Mysql database used for storing employees data.
--Tkinter(GUI programming toolkit)is used.

--create table employees in mysql________
create table employee(emp_id varchar(3),first_name varchar(10),last_name varchar(10),designation varchar(20), city varchar(20));